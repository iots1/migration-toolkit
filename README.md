# **🏥 HIS Database Migration Toolkit**

ชุดเครื่องมือสำหรับวิเคราะห์และตรวจสอบคุณภาพข้อมูล (Data Profiling) ของระบบฐานข้อมูลโรงพยาบาล (HIS) เพื่อเตรียมความพร้อมก่อนการทำ Data Migration รองรับ MySQL, PostgreSQL และ Microsoft SQL Server

## **🌟 ฟีเจอร์หลัก (Features)**

- **Multi-Database Support:** รองรับ MySQL, PostgreSQL, MSSQL
- **Auto-Detect Environment:** ตรวจสอบและสลับใช้ Bash version ล่าสุดอัตโนมัติ (แก้ปัญหา macOS เก่า)
- **Schema Export:** ดึงโครงสร้างตาราง (DDL) ออกมาเก็บไว้
- **Data Profiling:** วิเคราะห์ข้อมูลเชิงลึก (Row counts, Nulls, Max Length, Distinct)
- **Deep Analysis Mode:** วิเคราะห์ค่าต่ำสุด-สูงสุด และค่าที่พบบ่อยที่สุด (Top 5\)
- **Interactive HTML Report:**
  - **Overview Tab:** สรุปภาพรวมคุณภาพข้อมูลของแต่ละตาราง
  - **Detail Tab:** เจาะลึกรายคอลัมน์ พร้อมฟีเจอร์ Show/Hide Column
  - **Log Tab:** ตรวจสอบ Log การทำงานย้อนหลังได้ทันที

## **⚙️ การตั้งค่า (Configuration)**

แก้ไขไฟล์ config.json เพื่อกำหนดการเชื่อมต่อและพฤติกรรมการวิเคราะห์

```json
{
  "database": {
    "type": "postgresql",
    "host": "127.0.0.1",
    "port": "5432",
    "name": "his_db",
    "user": "admin",
    "password": "password"
  },
  "sampling": {
    "default_limit": 10, // จำนวน Sample Data ทั่วไป
    "max_text_length": 300, // ตัดข้อความตัวอย่างไม่ให้ยาวเกินนี้
    "deep_analysis": true, // เปิด/ปิด โหมดวิเคราะห์เชิงลึก (ดูรายละเอียดด้านล่าง)
    "exceptions": [
      // กฎพิเศษรายคอลัมน์
      { "table": "log_table", "column": "message", "limit": 3 }
    ]
  }
}
```

# 🧠 Deep Analysis Explained

การตั้งค่า `"deep_analysis": true` หรือ `false` ส่งผลต่อความเร็วและข้อมูลที่ได้ ดังนี้:

| Feature                    | `deep_analysis: false` (Basic)                                                                                                                                 | `deep_analysis: true` (Deep)                                                                                                                                               |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **ความเร็ว (Performance)** | ⚡ เร็วมาก (Fast)                                                                                                                                              | 🐢 ช้ากว่า (Slower) เนื่องจากต้อง Scan ข้อมูลทั้งตาราง                                                                                                                     |
| **การใช้ Resource**        | ต่ำ (Low Overhead)                                                                                                                                             | สูง (High I/O & CPU) โดยเฉพาะตารางใหญ่                                                                                                                                     |
| **Process ที่ทำ**          | 1. นับจำนวนแถวทั้งหมด<br>2. นับจำนวนค่า Null<br>3. หาความยาวข้อมูลสูงสุด (Max Len)<br>4. นับจำนวนค่าที่ไม่ซ้ำ (Distinct)<br>5. ดึงตัวอย่างข้อมูล (Sample Data) | ทำทุกอย่างเหมือน Basic และเพิ่ม:<br>6. Find Min/Max: หาค่าต่ำสุด/สูงสุด (เช็คช่วงวันที่, ค่าติดลบ)<br>7. Frequency Analysis: หา 5 อันดับค่าที่ใช้บ่อยที่สุด (Top 5 Values) |
| **เหมาะสำหรับ**            | การประเมินเบื้องต้น, ตาราง Log ขนาดใหญ่                                                                                                                        | การทำ Data Mapping, หาค่าผิดปกติ (Outlier), วิเคราะห์ Reference Data                                                                                                       |

## **🚀 วิธีการใช้งาน (Usage)**

1. **เตรียมไฟล์:** วางไฟล์ unified_db_analyzer.sh, csv_to_html.py, และ config.json ไว้โฟลเดอร์เดียวกัน
2. **รันคำสั่ง:**  
   chmod \+x unified_db_analyzer.sh  
   ./unified_db_analyzer.sh

3. **ดูผลลัพธ์:** ระบบจะสร้างโฟลเดอร์ migration_report/YYYYMMDD_HHMM/ และเปิดไฟล์ HTML ให้ทันที

## **📂 โครงสร้างไฟล์ผลลัพธ์ (Output Structure)**

migration_report/  
└── 20251122_2300/ \# Run ID (Timestamp)  
 ├── data_profile/  
 │ ├── data_profile.csv \# ข้อมูลดิบ CSV  
 │ └── data_profile.html \# Interactive Report (เปิดไฟล์นี้)  
 ├── ddl_schema/  
 │ └── schema.sql \# SQL สำหรับสร้างตาราง (Structure)  
 └── process.log \# บันทึกการทำงานและ Error ต่างๆ

## **📋 Requirements**

- **OS:** Linux, macOS, WSL (Windows)
- **Software:**
  - bash (version 4.0+ แนะนำ, script จะพยายามสลับให้เอง)
  - jq (สำหรับอ่าน JSON Config)
  - python3 (สำหรับสร้าง HTML Report)
  - Database Clients (mysql, psql, หรือ sqlcmd)
