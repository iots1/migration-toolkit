"""
Schema Mapper components — Phase 2 split targets.

    source_selector.py   render_source_selector()   — mode tabs (RunID/DS/Config/File)
    mapping_editor.py    render_mapping_editor()     — AgGrid table + quick-edit panel
    metadata_editor.py   render_config_metadata()    — config name, batch size, target selectors
    history_viewer.py    render_history_panel()      — version list + diff comparison
"""
